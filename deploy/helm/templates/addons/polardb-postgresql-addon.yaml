{{- $selectorLabels := include "kubeblocks.selectorLabels" . }}
{{- include "kubeblocks.buildAddonCR" (merge (dict
  "kbVersion" ">=0.8.0 <0.9.0"
  "selectorLabels" $selectorLabels
  "name" "polardb-postgresql"
  "version" "0.8.2"
  "model" "RDBMS"
  "provider" "community"
  "description" "Patroni-based PostgreSQL HA addon for KubeBlocks 0.8."
  "autoInstall" false) . ) -}}
